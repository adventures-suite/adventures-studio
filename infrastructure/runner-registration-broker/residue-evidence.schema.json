{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion","operationId","sourceSha","catalogSha256","entries","assignmentEvidenceSha256","result"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "operationId": { "type": "string", "pattern": "^broker-foundation-cleanup-[a-z0-9]{16,64}$" },
    "sourceSha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "catalogSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "entries": {
      "type": "array", "minItems": 23, "maxItems": 23,
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["id","type","state"],
        "properties": {
          "id": { "type": "string", "maxLength": 512 },
          "type": { "type": "string", "maxLength": 128 },
          "state": { "enum": ["VerifiedAbsent","SoftDeletedRetained"] }
        }
      }
    },
    "assignmentEvidenceSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "result": { "const": "Clean" }
  },
  "maxProperties": 8
}
