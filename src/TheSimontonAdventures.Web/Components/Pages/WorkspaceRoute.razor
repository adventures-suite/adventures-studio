@page "/workspace/{**WorkspacePath}"

@* The root App renders WorkspaceRoot only after canonical workspace-host
   classification. This routable marker makes explicitly addressed Planner
   URLs resolvable without establishing authorization in Razor routing. *@
